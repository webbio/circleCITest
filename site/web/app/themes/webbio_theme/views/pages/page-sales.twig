{% include 'partials/header-sales.twig' %}

<main class="uk-main">

  {% include 'components/section-sales-main.twig'%}

  {% include 'components/section-sales.twig'%}

  <div class="uk-section uk-padding-remove-top uk-padding-remove-bottom">
    <div class="uk-section-body">
      <div class="uk-container uk-container-small">
        <div class="uk-section-description" uk-grid>
          <div class="uk-width-2-5">
            <h2 class="uk-h1" style="font-size:40px;">{{ post.get_field('title_bottom') }}</h2>
          </div>
          <div class="uk-width-3-5">
            <div>
              {% if post.get_field('sub_title_bottom') %}
                {{ post.get_field('sub_title_bottom') }}
              {% endif %}
            </div>
          </div>
        </div>
      </div>
      <a href="{{ function('home_url', '/') }}" class="uk-logo uk-margin-auto uk-margin-large-top uk-margin-large-bottom">
        <img src="{{ options.navigatie_logo_kleur }}" uk-svg class="uk-preserve">
      </a>
  </div>

    {% include 'components/section-contact.twig'%}

</main>
